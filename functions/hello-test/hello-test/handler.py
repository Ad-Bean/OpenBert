import requests


def handle(req):
    """handle a request to the function
    Args:
        req (str): request body
    """
    body = '{"name": "John", "age": 30, "email": "john@example.com"}'

    return body
